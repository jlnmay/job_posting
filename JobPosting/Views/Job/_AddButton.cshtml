@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    <div class="row">
        <div class="col-md-offset-3 col-md-6">
            <button class="btn btn-primary" onclick="Jobs.callModal('N');">New Job Post</button>
        </div>
    </div>
}